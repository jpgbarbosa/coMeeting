<% if @admin %>
	<%= text_area :minutes, nil, :id => 'minutes', :value => @meeting.minutes , :onfocus => "handleFocus(this);" , :onblur => "handleBlur(this);"%>
<% else %>
	<%= text_area :minutes, nil, :id => 'minutes', :value => @minutes, :class => "autosize disabled", :disabled => true %>
<% end %>